# Stage 1 Agent Configuration
# Node runs under /sim namespace, so use /sim/stage1_agent
/sim/stage1_agent:
  ros__parameters:
    # Model path (in docker container)
    # Note: VecNormalize not needed - model was trained with norm_obs=False
    model_path: '/home/misys/overtake_agent/common/models/stage1/f1tenth_ppo_stage1_v1_20251110_133811/f1tenth_ppo_stage1_v1_20251110_133811_final.zip'

    # ROS2 topics
    scan_topic: '/sim/scan'
    odom_topic: '/sim/ego_racecar/odom'
    drive_topic: '/sim/drive'

    # Observation parameters
    num_beams: 1080
    frame_stack: 4
